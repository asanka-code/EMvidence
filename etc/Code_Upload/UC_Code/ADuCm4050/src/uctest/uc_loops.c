#include "uctest/uc_loops.h"
#include "include/adf_timers.h"

void simple_loop_10(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_9(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_8(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_7(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_6(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_5(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_4(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_3(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }

    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop_2(void) {

    // put your main code here, to run repeatedly:
    for (int i; i < 20; i++) {
        delay_ms(10);
    }
}

void simple_loop(void) {
    while (true) {

        delay_ms(1000); //
        delay_ms(1000); //
    }
}
